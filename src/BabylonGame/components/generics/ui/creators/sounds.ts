import { Sound } from '@babylonjs/core';
import { Hud } from '../ui';

//load all sounds needed for game ui interactions
export function _loadSounds(this: Hud): void {
  /*
  this._sounds._pause = new Sound(
    'pauseSong',
    './CarExplorer/sounds/Snowland.wav',
    this._scene,
    function () {
      // empty
    },
    {
      volume: 0.2,
    }
  );

  this._sounds._sfx = new Sound(
    'selection',
    './CarExplorer/sounds/vgmenuselect.wav',
    this._scene
  );

  this._sounds.quitSfx = new Sound(
    'quit',
    './CarExplorer/sounds/Retro Event UI 13.wav',
    this._scene
  );

  this._sounds._sparkWarningSfx = new Sound(
    'sparkWarning',
    './CarExplorer/sounds/Retro Water Drop 01.wav',
    this._scene,
    function () {
      // empty
    },
    {
      loop: true,
      volume: 0.5,
      playbackRate: 0.6,
    }
  );
  */
}
